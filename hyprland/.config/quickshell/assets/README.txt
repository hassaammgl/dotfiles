Drop GIFs/PNGs here for desktop anime widgets + empty states:

  session.gif   → default desktop mascot (kurukuru)
  media.gif     → bongo cat (cycle with left-click / ALT+SHIFT+A)
  dashboard.gif → alternate bongo
  empty.png     → dino (notifications + widget cycle)

Prefer transparent GIF/WebP, ~200–400px tall.
Toggle widgets: ALT+A  |  bar cat icon  |  qs ipc call widgets toggle
